<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>
  <p>
    <strong></strong>
    <img class='card-img-top' src="<%= category.icon %>">
  </p>
  <p>
    Created by <%= category.user.name %> <%= time_ago_in_words(category.created_at) %> ago
  </p>
</div>